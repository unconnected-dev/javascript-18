"use strict";



//Check if the global variable 'libraryName' is already defined before assigning it
//This prevents overwriting if another script has already set it
var libraryName = window.libraryName || "Lib 1";
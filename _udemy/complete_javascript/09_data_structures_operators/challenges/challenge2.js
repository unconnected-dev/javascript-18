'use strict';

//Game object is in challenge 1

// for(let [key, name] of Object.entries(game.scored)){
//     console.log(`Goal ${Number(key)+1}:  ${name}`);
// }



// let total = 0;
// for(let odd of Object.values(game.odds)){
//     total += odd;
// }

// let avg = total / Object.values(game.odds).length;
// console.log(avg);


// for(let [key, odd] of Object.entries(game.odds)){
//     if(key !== `x`)     console.log(`Odds of ${game[key]} victory: ${odd}`);
//     else                console.log(`Odds of draw: ${odd}`);
// }


// let scorers = {};
// for(let [key, name] of Object.entries(game.scored)){
    // scorers[name] === undefined ? scorers[name] = 1 : scorers[name] += 1;
    //If it exists
//     scorers[name] ? scorers[name] += 1 : scorers[name] = 1;
// }

// console.log(scorers);